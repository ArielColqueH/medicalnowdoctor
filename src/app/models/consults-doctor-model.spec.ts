import { ConsultsDoctorModel } from './consults-doctor-model';

describe('ConsultsDoctorModel', () => {
  it('should create an instance', () => {
    expect(new ConsultsDoctorModel()).toBeTruthy();
  });
});
