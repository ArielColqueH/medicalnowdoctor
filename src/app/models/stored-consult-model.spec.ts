import { StoredConsultModel } from './stored-consult-model';

describe('StoredConsultModel', () => {
  it('should create an instance', () => {
    expect(new StoredConsultModel()).toBeTruthy();
  });
});
