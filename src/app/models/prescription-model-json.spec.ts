import { PrescriptionModelJSON } from "./prescription-model-json";

describe("PrescriptionModelJSON", () => {
  it("should create an instance", () => {
    expect(new PrescriptionModelJSON()).toBeTruthy();
  });
});
